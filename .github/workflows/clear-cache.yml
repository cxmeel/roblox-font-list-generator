name: Weekly Cache Cleanup

on:
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:
  
jobs:
  clear-cache:
    runs-on: ubuntu-latest
    steps:
      - name: Delete font list cache
        run: gh cache delete temp-dir --confirm
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
